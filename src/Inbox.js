import React from 'react';
import './Inbox.css'; // Importing the CSS file

const Inbox = () => {
    return (
        <div className="inbox-container">
            <h1>Inbox</h1>
            {/* You can add more complex inbox functionalities here */}
            <p>Your messages will appear here.</p>
        </div>
    );
};

export default Inbox;
